# MCP server runtime dependencies (Streamlit intentionally removed)

# Model Context Protocol Python SDK (provides FastMCP + streamable-http transport)
mcp>=1.9.0

# Strands Agents SDK + tools (multi-agent pipelines, web_search tool)
strands-agents>=0.1.0
strands-agents-tools>=0.1.0

# AWS SDK (Bedrock, Bedrock Agent Runtime, S3)
boto3>=1.34.0

# Report Analysis Excel export support (report_forge)
openpyxl>=3.1.0

# .env loading for local runs
python-dotenv>=1.0.0
